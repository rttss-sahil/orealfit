import actionTypes from "../actions/actionTypes";

const initialState = "";

const currentCategory = (state = initialState, action) => {
  let newState;
  switch (action.type) {
    // case actionTypes.

    default:
      return state;
  }
};

export default currentCategory;
